<h2>
  List of all the Passengers
</h2>

<div class=index_list>
  <ol>
  <% @passengers.sort_by {|pass| pass.name}.each do |passenger|  %>
  <li>
    <%= link_to passenger.name, passenger_path(passenger.id) %>
  </li>
  <% end %>
</ul>

</div>
