<h1>List of All Passengers</h1>
<%= params.inspect %>

<ul>
  <% @passengers.each do |passenger| %>

  <li>
    <%= passenger.id %>)
      <%= passenger.name %>
      <%= passenger.phone_number %>
  </li>
    <% end %>
</ul>
