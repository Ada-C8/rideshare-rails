<h1> List of All Passengers! </h1>

<p>
  <%= link_to "add passenger", passengers_new_path %>
</p>

<p>
  <% @passengers.each do |passenger| %>
  <div class="passenger">
    <strong><%= link_to passenger.name, passenger_path(passenger.id) %></strong>
  </div>
  <% end %>
</p>
