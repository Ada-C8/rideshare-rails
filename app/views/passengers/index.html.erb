<h2> Passengers </h2>

<nav class="page-menu">
  <ul>
    <li>
      <%= link_to "New Passenger", new_driver_path %>
    </li>
  </ul>
</nav>

<ul class="index">
  <% @passengers.each do |passenger| %>
    <li>
      <strong>
        <%= link_to passenger.name, passenger_path(passenger.id) %>
      </strong>
    </li>
  <% end %>
</ul>
