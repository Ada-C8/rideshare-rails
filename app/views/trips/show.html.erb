<h1>Trip Details</h1>
<p>
  Date: <%= @trip.date %>
  Trip ID: <%= @trip.id %>
  Driver Name: <%= @trip.driver.name %>
  Passenger Name: <%= @trip.passenger.name %>
  Rating: <%= @trip.rating %>
  Cost: $<%='%.02f' % (@trip.cost / 100) %>

  <%= link_to "Edit Rating", edit_trip_path(@trip.id), alt: "Link to edit trip rating" %>
</p>
