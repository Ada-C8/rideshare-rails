<h2>ALL TRIPS</h2>
<ol class = 'trip_list'>
  <% @trips.order(date: :asc).each do |trip|%>
    <li class='single_trip'>

      <p>
        <strong >
          <%= link_to "Trip on: #{trip.date}", trip_path(trip) %>
        </strong>
      </p>
      <ul>
        <li>
          <%= "Driver: #{trip.driver.name}"%>
        </li>
        <li>
          <%= "Passenger: #{trip.passenger.name}"%>
        </li>
      </ul>

    </li>
    <% end %>
  </ol>
