<h1>Rideshare Drivers</h1>

<p>
   <%= link_to "Add Driver", new_driver_path %>
   <%= link_to "All Drivers", drivers_path %>
</p>
<table>
  <% @drivers.each do |driver| %>
  <tr>
    <td><%=link_to driver.name, driver_path(driver.id) %></td>
    <td>vin: <%= driver.vin %></td>
  </tr>
  <% end %>
</table>
