<h2>Add a new driver</h2>

<%= render partial: 'layouts/errors', locals: { model: @driver } %>

<%= render partial: "form", locals: { hide_placeholder_text: true, form_class: "blue", button_text: "Add a Driver!" }%>
