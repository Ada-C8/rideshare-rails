<article class="page">
  <h2 class="page-title">Add a New Driver</h2>

  <%= render partial: 'layouts/errors', locals: { model: @driver } %>

  <%= render partial: "form", locals: { hide_placeholder_text: true, button_text: "ADD" }%>
</article>
