<h1>Add a New Driver</h1>

<%= form_for @driver do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vin, "VIN" %>
  <%= f.text_field :vin %>

  <%= f.submit "Create a new driver" %>
<% end %>
