<h1>drivers#show</h1>

<h2>Params:<%= params %></h3>

<h3>Status:</h3>
<% if driver.available %>
  <p>Available</p>
  <%= link_to image_tag("Switch On.png"), driver_available_path, method: :patch %>
<% else %>
  <p>Unavailable</p>
  <%= link_to image_tag("Switch Off.png"), driver_available_path, method: :patch %>
<% end %>

<h2><%= link_to("Delete Driver", driver_path, method: :delete) %></h2>
<h2><%= link_to("Edit Driver", edit_driver_path)%></h2>
<h3>Driver: <%=@driver.name %> </h3>
<h3>Average Rating: <%=@driver.average_rating %></h3>
<h3>Vin: <%=@driver.vin %></h3>
<h3>Trips: <%= Trip.where(driver_id: @driver.id).find_each do |trip| %>
<h4>Trip ID:<%= link_to(trip.id, trip_path(trip.id)) %></h4>
<% end %>
<h3>Total Earnings: <%=@driver.total_earnings %></h3>
