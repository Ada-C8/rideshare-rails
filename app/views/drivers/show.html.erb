<h1>This is the driver summary page</h1>


<div>
</h2>Driver name: <%= @driver.name%></h2>
  <ul>
    <li>
      Driver's VIN: <%= @driver.vin%>
    </li>
    <li>
        Driver's Average Rating: <%= @driver.average_rating %>
    </li>
  <li>
      Driver's total earnings: $<%= "%.2f" % @driver.total_earned %>
  </li>
  <li>
    Driver Id: <%=@driver.id %>
  </li>
    </ul>
  <p>
  <h2>All the trips!</h2>

  <%trips = Trip.all.where(driver_id: @driver.id)%>
  <ol>
  <% trips.each do |trip| %>
    <li>
      <%passenger_name = Passenger.find(trip.passenger_id).name %>
      Trip ID: <%=link_to trip.id, trip_path(trip.id) %>
      Passenger: <%= link_to passenger_name, passenger_path(trip.passenger_id)%>
      Date: <%= trip.date %>
    </li>
  <%end%>
  </ol>






<%= link_to "Edit driver information", edit_driver_path(@driver.id), class: "link"%>
<%= link_to "Delete", driver_path(@driver.id), method: :delete, data: {confirm: "Are you sure you want to delete this driver from the database? You can not undo this action."}%>
</div>
