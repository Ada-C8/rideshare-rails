<%=link_to "Home", drivers_path %>
<hr />

<h2><%= "#{@driver.name}" %></h2>

<h3><%= "Average rating is #{@driver.average_rating} out of 5." %></h3>
<h3>Total Earnings: <%= money_format (@driver.total_earnings) %></h3>

<hr / />

</hr>
<% if !@trips.empty? %>
<% @trips.each do |trip| %>
Trip ID: <%= trip.id %>
<ul>
  <li>Date: <%=trip.date%>
  </li>
  <li>
    Passenger: <%= trip.passenger.name %>
  </li>
  <li>
    Rating: <%= trip.rating %>
  </li>
  <li>
    Earnings: <%= money_format(trip.cost) %>
  </li>
</ul>
<% end %>
<% else %>
<p>
  This driver has not given any rides, wow.  Help a driver out, will ya?
</p>

<% end %>
